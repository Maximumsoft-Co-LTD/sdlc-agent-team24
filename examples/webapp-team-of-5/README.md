# Example — webapp team of 5

A worked example showing what the skill produces for a typical small web-app team.

Run:

```
/sdlc-agent-template /Users/apaichon/libra/agentic-template/.claude/skills/sdlc-agent-template/examples/webapp-team-of-5/config.yaml
```

Expected output: a `chore-tracker/` directory with `.claude/agents/` containing `pm-agent.md`, `architect-agent.md`, `backend-dev-agent-1.md`, `backend-dev-agent-2.md`, `frontend-dev-agent.md`, `qa-agent.md`, `devops-agent.md`, `docs-agent.md`, `sdlc-orchestrator.md`; `.claude/skills/` containing the 14 helper skills; `AGENTS.md` at the root; `docs/sdlc/` with the full template scaffolding; `scripts/`; `CONVENTIONS.md`; `README.md`; `.gitignore`.
