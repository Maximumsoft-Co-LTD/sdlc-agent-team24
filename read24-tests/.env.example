# Copy to .env and set the deployed API base URL (no trailing slash).
# Post-deploy hook runs: unit + contract + E2E smoke (e2e/) when this is set.
# When unset, hook runs unit + contract only.
READ24_API_URL=http://localhost:3000

# Cloud Run example (used by post-deploy E2E smoke):
# READ24_API_URL=https://read24-292265115908.asia-southeast1.run.app
